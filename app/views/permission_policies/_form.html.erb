<%= form_with(model: permission_policy, local: true) do |form| %>
  <% if permission_policy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(permission_policy.errors.count, "error") %> prohibited this permission_policy from being saved:</h2>

      <ul>
      <% permission_policy.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <br/>


  <div class="field">
    <%= form.label :name , 'Nazwa' %>
    <%= form.text_field :name, class: "input" %>
  </div>

  <div class="field">
    <%= form.label Switch.find(1).name %>
    <%= form.check_box :io1 %>
  </div>

  <div class="field">
    <%= form.label Switch.find(2).name %>
    <%= form.check_box :io2 %>
  </div>

  <div class="field">
    <%= form.label Switch.find(3).name %>
    <%= form.check_box :io3 %>
  </div>

  <div class="field">
    <%= form.label Switch.find(4).name %>
    <%= form.check_box :io4 %>
  </div>

  <div class="actions">
    <%= form.submit(class: "button is-link" ) %>
  </div>
<% end %>
